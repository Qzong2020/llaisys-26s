# AGENTS.md

# LLAISYS Agent 工作规则

本文件中的规则在整个项目开发过程中持续有效。
开始任何工作前，应先阅读本文件和 `PROGRESS.md`。

---

## 1. 基本原则

1. 不允许修改、删除或放宽官方测试来让测试通过。

2. 不允许删除已有功能来解决当前问题。

3. 不允许通过 hardcode：
   - 固定 token
   - 固定测试输入
   - 固定测试输出
   - 针对特定测试数据写特殊分支
   等方式绕过正确实现。

4. 不允许为了当前任务进行无关的大规模重构。

5. 尽量保持项目已有：
   - API 设计
   - 目录结构
   - 命名方式
   - 代码风格
   - 错误处理方式

6. 优先进行最小必要修改。
   如果现有底层实现已经正确，只修复真正有问题的调用层或接口层。

---

## 2. 子任务执行规则

7. 一次只处理一个可独立验证的子任务。

8. 不要提前实现后续阶段的功能。

例如当前任务是：

“建立 Qwen2 C++ 模型骨架”

则不要顺便实现：
- 完整 Transformer forward
- KV Cache
- Python generate
- CUDA backend
- 与当前阶段无关的优化

9. 如果当前任务没有明确的子任务划分：

   - 先阅读任务要求；
   - 阅读相关源码；
   - 阅读测试；
   - 将任务拆成若干可独立验证的子任务；
   - 写入 `PROGRESS.md`；
   - 再开始实现。

10. 一个子任务必须有明确的完成条件。

不要把：
- “查看文件”
- “写一个循环”
- “添加 include”

当作独立里程碑。

应该使用例如：
- “Qwen2 C++ 模型可以成功创建和销毁”
- “339 个权重能够完整映射”
- “单次 prefill 得到正确 argmax”
- “KV Cache 增量 decode 与全量计算结果一致”

这样的可验证目标。

---

## 3. 不确定设计时的处理方式

11. 当不确定项目设计或语义时，按照以下优先级调查：

   1. `README_ZN.md` / 官方任务要求
   2. 官方测试
   3. 当前项目已有接口和调用代码
   4. 已有 CPU 实现
   5. 项目中的类似 backend / 类似模块
   6. 上游/reference 实现

12. 不要在没有调查现有实现的情况下自行设计新的公共 API。

13. 如果 README 没有要求新增接口，应优先判断能否使用已有接口完成任务。

14. “建议这样设计”不等于“必须修改”。

对于：
- 新增 API
- 新增依赖
- 改变生命周期
- 改变公开结构
- 大规模目录调整

如果不是完成当前任务所必需，应暂缓处理。

---

## 4. 测试与验证规则

15. 每完成一个可独立验证的子任务，都必须进行对应的最小可行验证。

16. 如果存在官方测试，必须优先运行对应官方测试。

17. 官方测试通过并不代表未覆盖场景一定正确。

如果当前任务需要官方测试未覆盖的功能，可以：

- 使用临时脚本；
- 使用一次性 Python/C++ 验证；
- 与 PyTorch/reference 结果比较；

但不要为了方便而修改官方测试。

18. 测试失败后，不要立刻盲目修改代码。

必须先：

   1. 阅读错误信息；
   2. 定位调用路径；
   3. 判断错误发生在哪一层；
   4. 分析根因；
   5. 提出最小修复方案；
   6. 再修改代码。

19. 修复 bug 后必须同时确认：

   - 新场景通过；
   - 原有相关测试仍然通过。

20. 不允许隐藏异常、跳过错误或删除检查来获得 PASS。

---

## 5. Task 3 特别规则

21. Task 3 当前目标是 CPU 上完成 Qwen2 /
DeepSeek-R1-Distill-Qwen-1.5B 推理。

22. 不要把 Task 4 的 NVIDIA/CUDA 实现提前混入 Task 3。

23. Task 3 最终核心验收目标是：

```bash
python test/test_infer.py --model <MODEL_PATH> --test